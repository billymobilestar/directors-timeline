import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Directors Timeline — Script Writer',
  description: 'Write or paste a script with screenplay formatting and export.',
};